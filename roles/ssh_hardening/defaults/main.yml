#SPDX-License-Identifier: MIT-0
---
# Default variables for the ssh_hardening role.
# Override these in group_vars/, host_vars/, or at the playbook level.

ssh_moduli_min_bits: 3071

# Groups allowed to connect via SSH
ssh_allow_groups:
  - teachers
  - ansible
