
                # Grafana HTTPS from teacher + teacher VPN networks → guard chain
                ip saddr @grafana_https_allowed_v4 tcp dport $GRAFANA_HTTPS_PORT tcp flags & (fin|syn|rst|ack) == syn ct state new jump grafana_https_guard_v4
