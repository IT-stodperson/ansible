
                # HTTP/HTTPS: only new SYN goes through guards (established handled in main chain)
                tcp dport $WEB_PORTS tcp flags & (fin|syn|rst|ack) == syn ct state new jump web_guard_v4
